import React from "react";
import { Link } from "react-router-dom";
import ImageCarousel from "../components/ImageCarousel";

const Home = () => {
  return (
    <div className="relative min-h-screen mt-8 bg-gradient-to-b from-yellow-50 via-white to-red-50 flex flex-col items-center text-center overflow-hidden">
      {/* Background Carousel */}
      <div className="w-full">
        <ImageCarousel />
      </div>

      {/* Hero Section */}
      <div className="flex flex-col items-center justify-center px-4 sm:px-8 md:px-16 mt-10 md:mt-14 text-center">
        <h1 className="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-extrabold text-red-700 mb-4 leading-tight drop-shadow-sm">
          Welcome to 🍴 TasteBite Restaurant
        </h1>

        <p className="max-w-md sm:max-w-xl md:max-w-2xl text-gray-700 text-sm sm:text-base md:text-lg mb-8 leading-relaxed">
          Enjoy the best cuisines in town — choose your table, explore our
          diverse menu, and order your favorite dishes in just a few clicks.
        </p>

        {/* Buttons */}
        <div className="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center items-center">
          <Link
            to="/menu"
            className="bg-red-600 hover:bg-red-700 text-white px-8 py-3 sm:px-10 sm:py-4 rounded-lg shadow-lg font-semibold text-sm sm:text-base md:text-lg transform hover:scale-105 transition-all duration-300"
          >
            🍛 View Menu
          </Link>
          <Link
            to="/order"
            className="bg-yellow-500 hover:bg-yellow-600 text-white px-8 py-3 sm:px-10 sm:py-4 rounded-lg shadow-lg font-semibold text-sm sm:text-base md:text-lg transform hover:scale-105 transition-all duration-300"
          >
            🛒 Place Order
          </Link>
        </div>

        {/* Restaurant Info */}
        <div className="mt-12 sm:mt-16 text-gray-600 text-sm sm:text-base leading-relaxed">
          <p>
            🕙 Open daily from{" "}
            <span className="font-semibold text-gray-800">
              10:00 AM - 11:00 PM
            </span>
          </p>
          <p className="mt-1">📍 123 Foodie Street, Delhi</p>
        </div>
      </div>

      {/* Decorative Footer */}
      <div className="absolute bottom-4 text-[11px] sm:text-xs md:text-sm text-gray-400">
        © {new Date().getFullYear()} TasteBite Restaurant — All Rights Reserved.
      </div>
    </div>
  );
};

export default Home;
