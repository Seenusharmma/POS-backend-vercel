frontend/src/
├── components/         # Shared UI components
│   ├── common/        # Common components (Navbar, Footer, BottomNav, Button)
│   ├── layout/        # Layout components (ProtectedRoute)
│   └── ui/            # UI components (LogoLoader, UsernameModal)
├── pages/             # Page components (Home, LoginPage, OrderHistory, Profile)
├── features/          # Feature-based components
│   ├── admin/         # Admin features
│   ├── home/          # Home page features
│   ├── menu/          # Menu features
│   └── orders/        # Order features
├── store/             # Redux store and slices
├── services/          # API services (api.js, firebase.js)
├── utils/             # Utility functions (socketConfig, polling, consoleSuppress)
├── config/            # Configuration files (api.js)
└── assets/            # Static assets